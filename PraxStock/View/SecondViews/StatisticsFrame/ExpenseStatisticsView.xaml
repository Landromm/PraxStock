<UserControl x:Class="PraxStock.View.SecondViews.StatisticsFrame.ExpenseStatisticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PraxStock.View.SecondViews.StatisticsFrame"
             xmlns:viewmodel="clr-namespace:PraxStock.ViewModel.SecondViewModel.StatisticsViewModel"
             d:DataContext="{d:DesignInstance Type=viewmodel:ExpenseStatisticsViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>

        <TextBlock
            FontSize="35"
            Foreground="Black"
            Margin="10"
            Text="Окно расходной статистики."/>
    </Grid>
</UserControl>
