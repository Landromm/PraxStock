<UserControl x:Class="PraxStock.View.SecondViews.StatisticsFrame.ExpenseStatisticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PraxStock.View.SecondViews.StatisticsFrame"
             xmlns:viewmodel="clr-namespace:PraxStock.ViewModel.SecondViewModel.StatisticsViewModel" 
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             d:DataContext="{d:DesignInstance Type=viewmodel:ExpenseStatisticsViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <UserControl.Resources>
        <Style x:Key="gridViewCollumnTextBlock" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="DemiBold"/>
            <Setter Property="Padding" Value="5 2"/>
        </Style>

        <Style x:Key="ToolTipTextBlock" TargetType="TextBlock">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="DemiBold"/>
            <Setter Property="Background" Value="{StaticResource color5Dark}"/>
        </Style>

        <Style x:Name="ListViewItemBase" TargetType="ListViewItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Foreground" Value="Black" />
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListViewItem}">
                        <Border
                            Name="Border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                            <GridViewRowPresenter Margin="{TemplateBinding Padding}" 
                                      Content="{TemplateBinding Content}" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="ItemsControl.AlternationIndex" Value="0">
                    <Setter Property="Background" Value="{StaticResource color5Default}" />
                </Trigger>
                <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                    <Setter Property="Background" Value="{StaticResource color6Default}" />
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource color8}" />
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource PraxixColor}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="TabItemHeader_TextBlock" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontStyle" Value="Italic"/>
            <Setter Property="Padding" Value="10 3"/>
        </Style>

        <!--#region Стиль для Label-->
        <Style TargetType="{x:Type Label}">
            <Setter Property="FontStyle" Value="Italic"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        <!--#endregion-->
        <!--#region Стиль для DatePickerTextBox-->
        <Thickness x:Key="BaseErrorBorderThickness">1</Thickness>

        <SolidColorBrush x:Key="ErrorBorderBrush" Color="Red"/>
        <SolidColorBrush x:Key="ErrorPolygonFillBrush" Color="Red"/>

        <Style TargetType="{x:Type DatePicker}" x:Key="BaseDatePickerStyle">
            <Setter Property="Padding" Value="5 2" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Focusable" Value="True" />
            <Setter Property="Background" Value="White"/>
            <Setter Property="Validation.ErrorTemplate">
                <Setter.Value>
                    <ControlTemplate>
                        <Grid>
                            <AdornedElementPlaceholder x:Name="adorner">
                                <Border 
                                    BorderBrush="{StaticResource ErrorBorderBrush}"
                                    BorderThickness="{StaticResource BaseErrorBorderThickness}" />
                            </AdornedElementPlaceholder>
                            <Polygon Fill="{StaticResource ErrorPolygonFillBrush}"
                                 Margin="0,2,2,0"
                                 Points="10,10 10,0 0,0"
                                 VerticalAlignment="Top"
                                 HorizontalAlignment="Right" >
                            </Polygon>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

            <Style.Resources>
                <Style TargetType="{x:Type Border}">
                    <Setter Property="CornerRadius" Value="5" />
                    <Setter Property="BorderBrush" Value="Black" />
                </Style>

                <Style x:Key="{x:Type ToolTip}" TargetType="{x:Type ToolTip}">
                    <Setter Property="Background" Value="White"/>
                    <Setter Property="Foreground" Value="{StaticResource ErrorPolygonFillBrush}"/>
                    <Setter Property="BorderBrush" Value="Black"/>
                    <Setter Property="BorderThickness" Value="{StaticResource BaseErrorBorderThickness}"/>
                </Style>
            </Style.Resources>

            <Style.Triggers>
                <Trigger Property="Validation.HasError" Value="true">
                    <Setter Property="ToolTip" Value="{Binding RelativeSource={x:Static RelativeSource.Self}, Path=(Validation.Errors)[0].ErrorContent}"/>
                    <Setter Property="Tag" Value="{Binding RelativeSource={x:Static RelativeSource.Self}, Path=(Validation.Errors)[0].ErrorContent}"/>
                </Trigger>
            </Style.Triggers>

        </Style>
        <!--#endregion-->
        <!--#region Стиль CheckBox-->
        <Style x:Key="FocusVisual">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Rectangle Margin="2" StrokeDashArray="1 2" Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" SnapsToDevicePixels="true" StrokeThickness="1"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="OptionMarkFocusVisual">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Rectangle Margin="14,0,0,0" StrokeDashArray="1 2" Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" SnapsToDevicePixels="true" StrokeThickness="1"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <SolidColorBrush x:Key="OptionMark.Static.Background" Color="#FFFFFFFF"/>
        <SolidColorBrush x:Key="OptionMark.Static.Border" Color="#FF707070"/>
        <SolidColorBrush x:Key="OptionMark.Static.Glyph" Color="#FF212121"/>
        <SolidColorBrush x:Key="OptionMark.MouseOver.Background" Color="#FFF3F9FF"/>
        <SolidColorBrush x:Key="OptionMark.MouseOver.Border" Color="#FF5593FF"/>
        <SolidColorBrush x:Key="OptionMark.MouseOver.Glyph" Color="#FF212121"/>
        <SolidColorBrush x:Key="OptionMark.Pressed.Background" Color="#FFD9ECFF"/>
        <SolidColorBrush x:Key="OptionMark.Pressed.Border" Color="#FF3C77DD"/>
        <SolidColorBrush x:Key="OptionMark.Pressed.Glyph" Color="#FF212121"/>
        <SolidColorBrush x:Key="OptionMark.Disabled.Background" Color="#FFE6E6E6"/>
        <SolidColorBrush x:Key="OptionMark.Disabled.Border" Color="#FFBCBCBC"/>
        <SolidColorBrush x:Key="OptionMark.Disabled.Glyph" Color="#FF707070"/>


        <Style x:Key="CheckBoxStatusStyle" TargetType="{x:Type CheckBox}">
            <Setter Property="FocusVisualStyle" Value="{StaticResource FocusVisual}"/>
            <Setter Property="Background" Value="{StaticResource OptionMark.Static.Background}"/>
            <Setter Property="BorderBrush" Value="{StaticResource OptionMark.Static.Border}"/>
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type CheckBox}">
                        <Grid x:Name="templateRoot" Background="Transparent" SnapsToDevicePixels="True">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border 
                                x:Name="checkBoxBorder" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                CornerRadius="4"
                                Margin="2" 
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}">
                                <Grid x:Name="markGrid">
                                    <Path 
                                        x:Name="optionMark" 
                                        Data="F1 M 9.97498,1.22334L 4.6983,9.09834L 4.52164,9.09834L 0,5.19331L 1.27664,3.52165L 4.255,6.08833L 8.33331,1.52588e-005L 9.97498,1.22334 Z " 
                                        Fill="{StaticResource OptionMark.Static.Glyph}" 
                                        Margin="3" 
                                        Opacity="0" 
                                        Stretch="None"/>
                                    <Rectangle x:Name="indeterminateMark" Fill="{StaticResource OptionMark.Static.Glyph}" Margin="2" Opacity="0"/>
                                </Grid>
                            </Border>
                            <ContentPresenter 
                                x:Name="contentPresenter" 
                                Grid.Column="1" 
                                Focusable="False" 
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                Margin="{TemplateBinding Padding}" 
                                RecognizesAccessKey="True" 
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" 
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="HasContent" Value="true">
                                <Setter Property="FocusVisualStyle" Value="{StaticResource OptionMarkFocusVisual}"/>
                                <Setter Property="Padding" Value="4,-1,0,0"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="Background" TargetName="checkBoxBorder" Value="{StaticResource OptionMark.MouseOver.Background}"/>
                                <Setter Property="BorderBrush" TargetName="checkBoxBorder" Value="{StaticResource OptionMark.MouseOver.Border}"/>
                                <Setter Property="Fill" TargetName="optionMark" Value="{StaticResource OptionMark.MouseOver.Glyph}"/>
                                <Setter Property="Fill" TargetName="indeterminateMark" Value="{StaticResource OptionMark.MouseOver.Glyph}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Background" TargetName="checkBoxBorder" Value="{StaticResource OptionMark.Disabled.Background}"/>
                                <Setter Property="BorderBrush" TargetName="checkBoxBorder" Value="{StaticResource OptionMark.Disabled.Border}"/>
                                <Setter Property="Fill" TargetName="optionMark" Value="{StaticResource OptionMark.Disabled.Glyph}"/>
                                <Setter Property="Fill" TargetName="indeterminateMark" Value="{StaticResource OptionMark.Disabled.Glyph}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="true">
                                <Setter Property="Background" TargetName="checkBoxBorder" Value="{StaticResource OptionMark.Pressed.Background}"/>
                                <Setter Property="BorderBrush" TargetName="checkBoxBorder" Value="{StaticResource OptionMark.Pressed.Border}"/>
                                <Setter Property="Fill" TargetName="optionMark" Value="{StaticResource OptionMark.Pressed.Glyph}"/>
                                <Setter Property="Fill" TargetName="indeterminateMark" Value="{StaticResource OptionMark.Pressed.Glyph}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="true">
                                <Setter Property="Opacity" TargetName="optionMark" Value="1"/>
                                <Setter Property="Opacity" TargetName="indeterminateMark" Value="0"/>
                                <Setter Property="BorderBrush" Value="{StaticResource color4Dark}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="{x:Null}">
                                <Setter Property="Opacity" TargetName="optionMark" Value="0"/>
                                <Setter Property="Opacity" TargetName="indeterminateMark" Value="1"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="False">
                                <Setter Property="BorderBrush" Value="{StaticResource OptionMark.Disabled.Glyph}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!--#endregion-->
    </UserControl.Resources>
    
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--Панель выбора дат промежутка отчетности-->
        <DockPanel
            Grid.Row="0"
            HorizontalAlignment="Stretch">

            <Label
                DockPanel.Dock="Left"
                Content="Промежуток отчетности - "
                VerticalAlignment="Bottom"
                Margin="5 0"/>

            <Label
                DockPanel.Dock="Left"
                Margin="0 0 5 0"
                Content="c:"
                VerticalAlignment="Bottom"
                FontWeight="Bold"/>

            <DatePicker
                DockPanel.Dock="Left"
                SelectedDate="{Binding StartDateStatistic, UpdateSourceTrigger=PropertyChanged}"
                FontSize="16"
                VerticalContentAlignment="Bottom"
                Height="30"
                Width="150"
                Style="{StaticResource BaseDatePickerStyle}"/>

            <Label
                DockPanel.Dock="Left"
                Margin="10 0 5 0"
                Content="по:"
                VerticalAlignment="Bottom"
                FontWeight="Bold"/>
            
            <DatePicker
                DockPanel.Dock="Left"
                SelectedDate="{Binding EndDateStatistic, UpdateSourceTrigger=PropertyChanged}"
                FontSize="16"
                VerticalContentAlignment="Bottom"
                Height="30"
                Width="150"
                Style="{StaticResource BaseDatePickerStyle}"/>

            <Button 
                DockPanel.Dock="Left"
                Margin="10 0"
                Style="{StaticResource iconButtonCatalog}"
                Tag="{StaticResource color2}"
                Command="{Binding GenerationStatisticsCommand}">

                <fa:IconImage 
                    Icon="Search" 
                    Style="{StaticResource iconButtonCatalogIcon}"/>

                <Button.ToolTip>
                    <TextBlock
                        Style="{StaticResource ToolTipTextBlock}"
                        Text="Поиск по заданным датам."/>
                </Button.ToolTip>
            </Button>
            
        </DockPanel>
        
        <!--Панель генерации отчета-->
        <DockPanel
            Grid.Row="1"
            HorizontalAlignment="Stretch">

            <CheckBox
                Style="{DynamicResource CheckBoxStatusStyle}"
                Margin="5 0 0 0"
                Padding="0"
                FontSize="14"
                HorizontalAlignment="Center"
                VerticalContentAlignment="Center"
                VerticalAlignment="Center"
                IsChecked="{Binding Path=StatusExport, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            
            <Label
                DockPanel.Dock="Left"
                Content="подробный отчет."
                VerticalAlignment="Center"
                FontSize="14"
                Margin="0 0"/>
            
            <Button 
                DockPanel.Dock="Left"
                HorizontalAlignment="Left"
                Margin="0 0"
                Style="{StaticResource iconButtonCatalog}"
                Tag="{StaticResource color6}"
                Command="{Binding GenerationReportCommand}">

                <fa:IconImage 
                    Icon="FileExcel"
                    IconFont="Solid"
                    Style="{StaticResource iconButtonCatalogIcon}"/>

                <Button.ToolTip>
                    <TextBlock
                        Style="{StaticResource ToolTipTextBlock}"
                        Text="Экспорт данных в Excel"/>
                </Button.ToolTip>
            </Button>

        </DockPanel>

        <!--Body-->
        <ListView   
            Grid.Row="2" 
            ItemsSource="{Binding StatisticMainCollection}"
            SelectedItem="{Binding SelectedStatisticMainCollection}"
            HorizontalAlignment="Stretch"
            Background="Transparent"
            BorderThickness="0" d:ItemsSource="{d:SampleData ItemCount=5}">

            <ListView.ItemContainerStyle>
                
                <Style TargetType="ListViewItem">

                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type ListViewItem}">
                                <Border 
                                    x:Name="BorderItemListView"
                                    BorderThickness="1"
                                    CornerRadius="5"
                                    HorizontalAlignment="Stretch"
                                    Margin="5 1">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="BorderBrush">
                                                <Setter.Value>
                                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                        <GradientStop Color="{StaticResource gradientGreenColor1Default}" Offset="0"/>
                                                        <GradientStop Color="{StaticResource gradientGreenColor2Default}" Offset="0.75"/>
                                                        <GradientStop Color="{StaticResource gradientGreenColor1White}" Offset="1"/>
                                                    </LinearGradientBrush>
                                                </Setter.Value>
                                            </Setter>
                                            <Setter Property="Background">
                                                <Setter.Value>
                                                    <LinearGradientBrush StartPoint="0,1" EndPoint="1,0">
                                                        <GradientStop Color="{StaticResource gradientGreenColor1White}" Offset="0"/>
                                                        <GradientStop Color="{StaticResource gradientGreenColor1Default}" Offset="1"/>
                                                    </LinearGradientBrush>
                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>

                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="{StaticResource color6Default}"/>
                                                    <Setter Property="BorderBrush" Value="{StaticResource panelColorDark}"/>
                                                </Trigger>

                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>

                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition/>
                                            <ColumnDefinition Width="100"/>
                                            <ColumnDefinition/>
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition/>
                                            <RowDefinition/>
                                        </Grid.RowDefinitions>
                                        
                                        <!--#region Основная информация по позиции-->
                                        <Label
                                            Content="Наименование:"
                                            Grid.Column="0"
                                            Grid.Row="0"
                                            FontSize="12"/>

                                        <Border
                                            Grid.Column="0"
                                            Grid.Row="1"
                                            BorderThickness="1" 
                                            BorderBrush="{StaticResource PraxixColor}" 
                                            CornerRadius="3"
                                            Padding="5 3"
                                            Margin="5 5 10 5"
                                            HorizontalAlignment="Left">
                                            <TextBlock 
                                                Text="{Binding NamePosition}"
                                                TextAlignment="Left"
                                                VerticalAlignment="Center"
                                                FontSize="16"
                                                FontWeight="DemiBold"/>
                                        </Border>

                                        <Label
                                            Content="Ед.изм."
                                            Grid.Column="1"
                                            Grid.Row="0"
                                            FontSize="12"/>

                                        <TextBlock 
                                            Grid.Column="1"
                                            Grid.Row="1"
                                            Margin="5"
                                            Text="{Binding UnitMeasure}"
                                            TextAlignment="Left"
                                            VerticalAlignment="Center"
                                            FontSize="16"
                                            FontWeight="DemiBold"/>

                                        <Label
                                            Content="Списано (Сумма):"
                                            Grid.Column="2"
                                            Grid.Row="0"
                                            FontSize="12"/>

                                        <TextBlock 
                                            Grid.Column="2"
                                            Grid.Row="1"
                                            Margin="5"
                                            Text="{Binding MoveInPostSumm}"
                                            TextAlignment="Left"
                                            VerticalAlignment="Center"
                                            FontSize="16"
                                            FontWeight="DemiBold"/>
                                        <!--#endregion-->
                                        
                                    </Grid>

                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type ListViewItem}">
                                        <Border 
                                            x:Name="BorderItemListView"
                                            BorderThickness="2"
                                            CornerRadius="5"
                                            HorizontalAlignment="Stretch"
                                            Margin="5 1">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="BorderBrush" Value="{StaticResource panelColorDark}"/>
                                                    <Setter Property="Background" Value="{StaticResource color5Default}"/>
                                                </Style>
                                            </Border.Style>

                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition/>
                                                    <ColumnDefinition Width="100"/>
                                                    <ColumnDefinition/>
                                                </Grid.ColumnDefinitions>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition/>
                                                    <RowDefinition/>
                                                    <RowDefinition/>
                                                    <RowDefinition/>
                                                </Grid.RowDefinitions>

                                                <!--#region Основная информация по позиции-->
                                                <Label
                                                    Content="Наименование:"
                                                    Grid.Column="0"
                                                    Grid.Row="0"
                                                    FontSize="12"/>

                                                <Border
                                                    Grid.Column="0"
                                                    Grid.Row="1"
                                                    BorderThickness="1" 
                                                    BorderBrush="{StaticResource PraxixColor}" 
                                                    CornerRadius="3"
                                                    Padding="5 3"
                                                    Margin="5 5 10 5"
                                                    HorizontalAlignment="Left">
                                                    <TextBlock 
                                                        Text="{Binding NamePosition}"
                                                        TextAlignment="Left"
                                                        VerticalAlignment="Center"
                                                        FontSize="16"
                                                        FontWeight="DemiBold"/>
                                                </Border>

                                                <Label
                                                    Content="Ед.изм."
                                                    Grid.Column="1"
                                                    Grid.Row="0"
                                                    FontSize="12"/>

                                                <TextBlock 
                                                    Grid.Column="1"
                                                    Grid.Row="1"
                                                    Margin="5"
                                                    Text="{Binding UnitMeasure}"
                                                    TextAlignment="Left"
                                                    VerticalAlignment="Center"
                                                    FontSize="16"
                                                    FontWeight="DemiBold"/>

                                                <Label
                                                    Content="Списано (Сумма):"
                                                    Grid.Column="2"
                                                    Grid.Row="0"
                                                    FontSize="12"/>

                                                <TextBlock 
                                                    Grid.Column="2"
                                                    Grid.Row="1"
                                                    Margin="5"
                                                    Text="{Binding MoveInPostSumm}"
                                                    TextAlignment="Left"
                                                    VerticalAlignment="Center"
                                                    FontSize="16"
                                                    FontWeight="DemiBold"/>

                                                <Label
                                                    Content="Подробная информация по списаниям:"
                                                    Grid.Column="0"
                                                    Grid.Row="2"
                                                    FontSize="12"/>
                                                <!--#endregion-->
                                                
                                                <!--#region Таблица подробной информации-->
                                                <Border
                                                    Grid.ColumnSpan="3"
                                                    Grid.Row="3"
                                                    BorderThickness="1"
                                                    CornerRadius="5"
                                                    BorderBrush="{StaticResource PraxixColor}"
                                                    Margin="5 3">

                                                    <ListView 
                                                        Grid.Row="2" 
                                                        ItemsSource="{Binding MoveListItems}"
                                                        SelectionMode="Extended"
                                                        HorizontalAlignment="Stretch"
                                                        Background="{StaticResource color6Default}"
                                                        BorderThickness="0" 
                                                        AlternationCount="2"
                                                        Margin="2"
                                                        d:ItemsSource="{d:SampleData ItemCount=5}"
                                                        HorizontalContentAlignment="Center" 
                                                        Grid.IsSharedSizeScope="True">

                                                        <ListView.View>
                                                            <GridView>
                                                                <GridViewColumn 
                                                                    DisplayMemberBinding="{Binding Path=IdMove}">
                                                                    <TextBlock
                                                                        Style="{StaticResource gridViewCollumnTextBlock}"
                                                                        Text="№"/>
                                                                </GridViewColumn>
                                                                <GridViewColumn>
                                                                    <TextBlock
                                                                        Style="{StaticResource gridViewCollumnTextBlock}"
                                                                        Text="Наименование"/>
                                                                    <GridViewColumn.CellTemplate>
                                                                        <DataTemplate>
                                                                            <Border 
                                                                                BorderThickness="1" 
                                                                                BorderBrush="{StaticResource panelActiveColorDark}" 
                                                                                CornerRadius="3"
                                                                                Padding="5 3">
                                                                                <TextBlock 
                                                                                    Text="{Binding Name}"
                                                                                    TextAlignment="Left"
                                                                                    VerticalAlignment="Center"
                                                                                    FontSize="14"
                                                                                    FontWeight="DemiBold"/>
                                                                            </Border>
                                                                        </DataTemplate>
                                                                    </GridViewColumn.CellTemplate>
                                                                </GridViewColumn>
                                                                <GridViewColumn 
                                                                    DisplayMemberBinding="{Binding Path=UnitMeasure}">
                                                                    <TextBlock
                                                                        Style="{StaticResource gridViewCollumnTextBlock}"
                                                                        Text="Ед.измер."/>
                                                                </GridViewColumn>
                                                                <GridViewColumn>
                                                                    <TextBlock
                                                                        Style="{StaticResource gridViewCollumnTextBlock}"
                                                                        Text="Количество"/>
                                                                    <GridViewColumn.CellTemplate>
                                                                        <DataTemplate>
                                                                            <Border 
                                                                                BorderThickness="1" 
                                                                                BorderBrush="{StaticResource panelActiveColorDark}" 
                                                                                CornerRadius="3"
                                                                                Padding="5 3"
                                                                                HorizontalAlignment="Left">
                                                                                <TextBlock 
                                                                                    Text="{Binding Path=UnitCount}"
                                                                                    TextAlignment="Left"
                                                                                    VerticalAlignment="Center"
                                                                                    FontSize="14"
                                                                                    FontWeight="DemiBold"/>
                                                                            </Border>
                                                                        </DataTemplate>
                                                                    </GridViewColumn.CellTemplate>
                                                                </GridViewColumn>
                                                                <GridViewColumn 
                                                                    DisplayMemberBinding="{Binding Path=DateMove, StringFormat=dd-MM-yyyy}">
                                                                    <TextBlock
                                                                        Style="{StaticResource gridViewCollumnTextBlock}"
                                                                        Text="Дата перемещения на пост"/>
                                                                </GridViewColumn>
                                                                <GridViewColumn>
                                                                    <TextBlock
                                                                        Style="{StaticResource gridViewCollumnTextBlock}"
                                                                        Text="Пост"/>
                                                                    <GridViewColumn.CellTemplate>
                                                                        <DataTemplate>
                                                                            <Border 
                                                                                BorderThickness="1" 
                                                                                BorderBrush="{StaticResource panelActiveColorDark}" 
                                                                                CornerRadius="3"
                                                                                Padding="5 3">
                                                                                <TextBlock 
                                                                                    Text="{Binding Path=NamePost}"
                                                                                    TextAlignment="Left"
                                                                                    VerticalAlignment="Center"
                                                                                    FontSize="14"
                                                                                    FontWeight="DemiBold"/>
                                                                            </Border>
                                                                        </DataTemplate>
                                                                    </GridViewColumn.CellTemplate>
                                                                </GridViewColumn>
                                                            </GridView>
                                                        </ListView.View>

                                                        <ListView.ItemContainerStyle>

                                                            <Style TargetType="ListViewItem">
                                                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                                                <Setter Property="Foreground" Value="Black" />
                                                                <Setter Property="FontSize" Value="14"/>
                                                                <Setter Property="Template">
                                                                    <Setter.Value>
                                                                        <ControlTemplate TargetType="{x:Type ListViewItem}">
                                                                            <Border
                                                                                Name="Border"
                                                                                Background="{TemplateBinding Background}"
                                                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                                                BorderThickness="{TemplateBinding BorderThickness}">
                                                                                <GridViewRowPresenter Margin="{TemplateBinding Padding}" 
                                                                                    Content="{TemplateBinding Content}" />
                                                                            </Border>
                                                                        </ControlTemplate>
                                                                    </Setter.Value>
                                                                </Setter>
                                                                <Style.Triggers>
                                                                    <Trigger Property="ItemsControl.AlternationIndex" Value="0">
                                                                        <Setter Property="Background" Value="{StaticResource color15}" />
                                                                    </Trigger>
                                                                    <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                                                                        <Setter Property="Background" Value="{StaticResource color14}" />
                                                                    </Trigger>
                                                                    <!--<Trigger Property="IsSelected" Value="True">
                                                                        <Setter Property="Background" Value="{StaticResource color8}" />
                                                                    </Trigger>-->
                                                                    <Trigger Property="IsMouseOver" Value="True">
                                                                        <Setter Property="Background" Value="{StaticResource PraxixColor}"/>
                                                                    </Trigger>

                                                                </Style.Triggers>
                                                            </Style>

                                                        </ListView.ItemContainerStyle>

                                                    </ListView>

                                                </Border>
                                                <!--#endregion-->

                                            </Grid>

                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </Style.Triggers>

                </Style>

            </ListView.ItemContainerStyle>

        </ListView>

    </Grid>
</UserControl>
